{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    
        {# <div class="max-w-2xl mx-auto border py-12 my-4 px-20 mt-[120px] bg-white rounded-lg">
            <h1 class='text-center text-3xl mb-6'>S'enregistrer</h1>
        <div class="mx-auto w-full max-w-md">
        {{ form_start(registrationForm) }}
        {{ form_row(registrationForm.name, {'label': 'Nom d\'utilisateur'}) }}
        {{ form_row(registrationForm.email, {'label': 'Adresse email'}) }}
        {{ form_row(registrationForm.plainPassword.first, {'label': 'Mot de passe'}) }}
        {{ form_row(registrationForm.plainPassword.second, {'label': 'Confirmez le mot de passe'}) }}
        <button formnovalidate  class="bg-slate-500 text-white py-2 px-3 hover:bg-slate-400 rounded-lg mt-5 w-full" type="submit">Valider</button>
        {{ form_end(registrationForm) }}
    </div>
</div> #}
    <div class="max-w-2xl mx-auto border py-12 my-4 px-20 mt-[120px] bg-white rounded-lg">
    <h1 class='text-center text-3xl mb-6'>S'enregistrer</h1>
    <div class="mx-auto w-full max-w-md">
        {{ form_start(registrationForm) }}
        <div class="mb-4">
            {{ form_label(registrationForm.name, 'Nom d\'utilisateur', {'label_attr': {'class': 'block text-gray-700 font-bold mb-2'}}) }}
            {{ form_widget(registrationForm.name, {'attr': {'class': 'form-input w-full rounded-md shadow-sm'}}) }}
        </div>
        <div class="mb-4">
            {{ form_label(registrationForm.email, 'Adresse email', {'label_attr': {'class': 'block text-gray-700 font-bold mb-2'}}) }}
            {{ form_widget(registrationForm.email, {'attr': {'class': 'form-input w-full rounded-md shadow-sm'}}) }}
        </div>
        <div class="mb-4">
            {{ form_label(registrationForm.plainPassword.first, 'Mot de passe', {'label_attr': {'class': 'block text-gray-700 font-bold mb-2'}}) }}
            {{ form_widget(registrationForm.plainPassword.first, {'attr': {'class': 'form-input w-full rounded-md shadow-sm'}}) }}
        </div>
        <div class="mb-4">
            {{ form_label(registrationForm.plainPassword.second, 'Confirmez le mot de passe', {'label_attr': {'class': 'block text-gray-700 font-bold mb-2'}}) }}
            {{ form_widget(registrationForm.plainPassword.second, {'attr': {'class': 'form-input w-full rounded-md shadow-sm'}}) }}
        </div>
        <button formnovalidate  class="bg-slate-500 text-white py-2 px-3 hover:bg-slate-400 rounded-lg mt-5 w-full mb-3" type="submit">Valider</button>
        {{ form_end(registrationForm) }}
    </div>
</div>


  
    
{% endblock %}
