{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
        {% for user in users %}

            <div>
                <img src="{{user.avatar}}" alt="">
                <span>{{user.name}}</span>
                
                
            </div>
            
        {% endfor %}
{% endblock %}
